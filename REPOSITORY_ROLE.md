# Repository Role

`linguamesh-core` owns provider-neutral domain behavior, provider adapters, model routing, translation orchestration, streaming, cancellation, persistence, document codecs, stable protocol contracts, the C ABI, generated native bindings, the reference CLI, fake provider, fixtures, tests, fuzz targets, and benchmarks.

It does not own native application UI, platform credential storage, file pickers, notifications, or platform lifecycle behavior.
